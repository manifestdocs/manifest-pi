/**
 * Thin package entrypoint for Pi.
 *
 * The real implementation lives in src/ and is compiled to dist/ so the
 * extension code is covered by TypeScript checks and tests.
 */

export { default } from '../dist/feature-workflow.js';
